import 'package:flutter/material.dart';

import 'design_lab/gallery.dart';

/// Design Lab entry point — pokreni sa:
/// flutter run -t lib/design_lab_main.dart -d chrome
void main() => runApp(const DesignLabApp());
